# Cuy::Rails

Rails integration for Cuy. Adds AR inference, route helpers, polymorphic paths.

Requires **cuy** gem.

## Rails components

ModelTable, ModelShow, ModelFilterBar, IndexView, EnumSelect, FieldType resolver, Layout (CSRF/assets), Navbar (route resolution).
