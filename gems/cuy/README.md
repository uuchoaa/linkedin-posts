# Cuy

Framework-agnostic design system. Phlex components that produce HTML.

Use with Sinatra, Rack, or **cuy-rails** for Rails.

## Components

Badge, Button, Select, Table, Form, Form::Grid, Form::Section, Form::Actions, FilterBar, PageHeader, Layout, Navbar, PageView, FieldTypes::*.
